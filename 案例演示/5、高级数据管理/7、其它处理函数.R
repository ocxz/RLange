# 其它对于数据管理和处理的函数
# 1、求对象长度：length(x)
# 2、生成一个序列：seq(from, to, by)   生成等差向量
# 3、重复元素：rep(x, n)   生成重复n次x的向量
# 4、分割变量为因子：cut(x, n)   使用选项ordered_result=TRUE 可创建一个有序型因子
# 5、创建美观分割的：pretty(x, n)  通过选取n+1个等间距取整，将一个连续型变量x分割成n个区间
# 6、cat(……, file="myfile", append=FALSE) 连接……中的对象，将其输出到屏幕或文件中

